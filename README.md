# Fitness Coach Agent

Multi-agent sports-health coach (LangGraph + RAG). See `docs/COACH_AGENT_REDESIGN.md`.

## Quick start

```bash
docker compose up -d
cp backend/.env.example backend/.env
# set DASHSCOPE_API_KEY in backend/.env
cd backend && pip install -r requirements.txt
```
